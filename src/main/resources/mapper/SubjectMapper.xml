<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lx.edusphere_server.mapper.SubjectMapper">

    <insert id="add_one_subject">
        INSERT INTO subjects(subject_name, subject_desc) VALUES (#{subject_name},#{subject_desc});
    </insert>

    <select id="get_all_subjects">
        SELECT subject_id, subject_name, subject_desc FROM subjects;
    </select>

    <update id="update_subject">
        UPDATE subjects SET subject_name = #{subject_name}, subject_desc = #{subject_desc}
        WHERE subject_id = #{subject_id}
    </update>

    <select id="is_there_a_class_that_has_this_course">
        SELECT COUNT(*) > 0 FROM class_teachers WHERE subject_id = #{subject_id};
    </select>

    <delete id="delete_subject">
        DELETE FROM subjects WHERE subject_id = #{subject_id};
    </delete>

</mapper>
