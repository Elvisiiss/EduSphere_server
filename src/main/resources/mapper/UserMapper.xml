<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lx.edusphere_server.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.lx.edusphere_server.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="role_id" property="roleId" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="VARCHAR"/>
        <result column="updated_at" property="updatedAt" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insert" parameterType="com.lx.edusphere_server.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users(user_name, email, password, role_id, created_at, updated_at)
        VALUES(#{userName}, #{email}, #{password}, #{roleId}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.lx.edusphere_server.entity.User">
        UPDATE users SET
                         user_name = #{userName},
                         email = #{email},
                         password = #{password},
                         role_id = #{roleId},
                         updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findByUserName" resultMap="BaseResultMap">
        SELECT * FROM users WHERE user_name = #{userName}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

    <select id="existsByUserName" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE user_name = #{userName}
    </select>
</mapper>