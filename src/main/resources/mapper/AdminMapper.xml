<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lx.edusphere_server.mapper.AdminMapper">

    <select id="selectAll" resultType="com.lx.edusphere_server.entity.User">
        SELECT * FROM users
    </select>

    <delete id="deleteById">
        DELETE FROM users WHERE user_id = #{user_id}
    </delete>

    <select id="selectById" resultType="com.lx.edusphere_server.entity.User">
        SELECT user_id,user_name,user_email,user_token FROM users WHERE user_id = #{user_id}
    </select>
</mapper>